# This file is compatible with 'pip install -r requirements.txt'
# It reflects the new 2024-2025 SOTA model architecture and legacy tools.

# --- Core Frameworks ---
agno
fastapi
uvicorn
pydantic

# --- LLM & Embedding Models ---
openai
anthropic
sentence-transformers
torch
transformers[video]
Pillow
# For performance optimization (quantization)
bitsandbytes
optimum
# For faster attention mechanism (install separately if needed)
# pip install flash-attn --no-build-isolation
flash-attn
# For models like LTX-Video (if used for generation/enhancement)
diffusers[torch]
# Required by many vision models like DETR
timm

# --- Utility Video/Audio Processing ---
# Still useful for low-level tasks like scene boundary detection
scenedetect[opencv]
opencv-python
moviepy==1.0.3
# For optimized audio loading and processing
librosa
# For noise reduction in audio
noisereduce

# --- Databases & Storage ---
lancedb
neo4j
asyncpg
sqlalchemy[asyncio]
redis[hiredis]

# --- Testing Framework ---
pytest
pytest-mock
pytest-cov
pytest-asyncio

#regex
regex